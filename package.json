{
  "scripts": {
    "start": "node server.mjs",
    "build": "npm run build:photography && npm run build:seo",
    "build:photography": "node scripts/build-photography.mjs",
    "build:seo": "node scripts/build-sitemap.mjs",
    "check:seo": "node scripts/check-seo.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
